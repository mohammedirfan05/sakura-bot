# 🌸 Sakura Bot — Python Dependencies

# Core Discord library
discord.py>=2.4.0

# Async SQLite (database)
aiosqlite>=0.20.0

# Environment variable loading
python-dotenv>=1.0.1

# Async HTTP client (fun commands, future API integrations)
aiohttp>=3.9.5

# Image generation (rank cards — Phase 2)
Pillow>=10.4.0

# AI integration (Groq — free tier, high rate limits)
groq>=0.9.0