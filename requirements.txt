# 🌸 Sakura Bot — Python Dependencies

# Core Discord library
discord.py>=2.4.0

# Async SQLite (database)
aiosqlite>=0.20.0

# Environment variable loading
python-dotenv>=1.0.1

# Async HTTP client — also used for Groq AI REST calls
aiohttp>=3.9.5

# Image generation (rank cards)
Pillow>=10.4.0